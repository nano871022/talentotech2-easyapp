# Desactivar listado de directorios
Options -Indexes

# Proteger carpeta admin (si quieres forzar HTTPS y reescrituras)
<IfModule mod_rewrite.c>
  RewriteEngine On
  # Forzar HTTPS (si host soporta)
  RewriteCond %{HTTPS} !=on
  RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
