name: Advise Service CI/CD

on:
  workflow_dispatch:
  workflow_call:

#concurrency:
#  group: ${{ github.workflow }}-${{ github.ref }}
#  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/reusable-test.yml
    with:
      service_name: advise-service

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: Install Composer dependencies
        run: |
          cd backend/advise-service
          composer install --no-dev --optimize-autoloader

      - name: Copy Core and Middleware directories
        run: |
          cp -r backend/app/Core backend/advise-service/app/Core
          cp -r backend/app/Middleware backend/advise-service/app/Middleware

      - name: Create deployment package
        run: |
          cd backend/advise-service
          zip -r deployment.zip . -x "*.git*"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: advise-deployment-package
          path: backend/advise-service/deployment.zip

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: advise-deployment-package

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to AWS Lambda
        run: aws lambda update-function-code --function-name ${{ secrets.ADVISE_LAMBDA_FUNCTION_NAME }} --zip-file fileb://deployment.zip
